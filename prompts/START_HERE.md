# Prompt inicial recomendado

Abre Claude Code en la raíz y ejecuta:

```text
/implement-phase 00
```

Si prefieres lenguaje natural:

```text
Implementa únicamente la fase 00 siguiendo CLAUDE.md y plans/phase-00-bootstrap.md. Trabaja en slices pequeños, ejecuta todos los gates y actualiza PROGRESS.md y HANDOFF.md. No avances a fase 01.
```

Después de Phase 00, si `graphify` está instalado, construir/consultar el grafo del repo según `docs/22-codebase-graph.md` y usarlo para orientación en fases posteriores.
